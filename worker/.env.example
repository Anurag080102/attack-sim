# Redis broker
BROKER_URL=redis://localhost:6381/0
CELERY_QUEUE=attack_sim

# JWT / token
JWT_ALG=HS256
JWT_SECRET=replace-me-with-secret

# Backend API (for callbacks later)
API_BASE_URL=http://localhost:8000
